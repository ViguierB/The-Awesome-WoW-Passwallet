{
  "name": "the-awesome-wow-passwallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build-native-dev": "rm -r build; electron-rebuild --debug -f -w native_executor",
    "build-native": "rm -r build; electron-rebuild -f -w native_executor",
    "build-all": "npm run build-native && npm run build",
    "build-all-dev": "npm run build-native-dev && npm run build",
    "start-dev": "env IS_DEV=true electron --js-flags='--expose-gc' --inspect=5858 ./dist/main.js",
    "start": "electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.holidev.net:ben/the-awesome-wow-passwallet.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.5",
    "electron-rebuild": "^2.3.2",
    "node-addon-api": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "crypto": "^1.0.1",
    "keytar": "^7.0.0",
    "nan": "^2.14.2",
    "segfault-handler": "^1.3.0"
  }
}
